---
- name: Update APT cache
  apt:
    update_cache: "{{ allow_update_apt_cache }}" # true

- name: Install Nginx
  apt:
    name: nginx
    state: present

- name: Make sure a service unit is running
  ansible.builtin.systemd_service:
    state: started
    name: nginx
